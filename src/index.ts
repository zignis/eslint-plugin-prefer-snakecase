// noinspection JSUnusedGlobalSymbols

import { recommended } from "./configs/recommended";
import { snake_case } from "./rules";

export const configs = {
  recommended,
};

export const rules = {
  "prefer-snakecase": snake_case,
};
